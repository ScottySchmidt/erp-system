import { createFileRoute } from "@tanstack/react-router";
import { json } from "@tanstack/react-start";

export const Route = createFileRoute("/api/supabase-check")({
  server: {
    handlers: {
      GET: async () => json({ ok: true, route: "api/supabase-check" }),
    },
  },
});